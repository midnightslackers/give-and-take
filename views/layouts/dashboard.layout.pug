doctype html
html(lang="en")
  head
    if pageTitle
      title #{pageTitle} - Give and Take
    else
      title Give and Take
    link(rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css")
    link(rel="stylesheet" href="/styles/master.css")
  body.page--dashboard
    div.container-fluid
      header
        nav.navbar.navbar-default
          div.navbar-header
            button.navbar-toggle.collapsed(data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false")
              span.sr-only toggle navigation
              span.icon-bar
              span.icon-bar
              span.icon-bar
            a.navbar-brand(href="/dashboard") Give and Take
          div.collapse.navbar-collapse#navbar-collapse
            ul.nav.navbar-nav.navbar-right
              li
                a(href="#")= UserName
              li
                a(href="/about") About
              li
                a(href="#") Log Out
      aside
        h2 Filter
        div.form-group
          label(for="Filter-Topics") Topics
          select.form-control#Filter-Topics(name="topics")
        div.form-group
          label(for="Filter-Subtopics") Subtopics
          select.form-control#Filter-Subtopics(name="subtopics")
        div.form-group
          label(for="Filter-Gender") Gender
          div.radio
            label(for="Filter-Gender-Female")
              input#Filter-Gender-Female(type="radio" name="gender" value="female")
              span Female
            label(for="Filter-Gender-Male")
              input#Filter-Gender-Male(type="radio" name="gender" value="Male")
              span Male
      main
        block content
    script(src="//code.jquery.com/jquery-1.12.4.min.js")
    script(src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js")
    script(src="//cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.0.5/handlebars.min.js")
    script(src="/scripts/master.js")
